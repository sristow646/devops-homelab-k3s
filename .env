# === Zertifikatspfad für TLS Wildcard-Zertifikat (z. B. *.privat.de) ===
TLS_CERT_PATH=/pfad/zu/deinem/wildcard-zertifikat.crt
TLS_KEY_PATH=/pfad/zu/deinem/wildcard-key.key

# === MetalLB IP Range für LoadBalancer Services im Homelab ===
# Beispiel: IP-Bereich im privaten LAN für Ingress-NGINX LoadBalancer
METALLB_IP_RANGE=192.168.xxx.xxx-192.168.xxx.xxx

# === Ingress-Domains für die Homelab-Services ===
# Alle Hosts müssen RFC1123-konform sein und im DNS lokal auf die MetalLB IP zeigen (z. B. Fritz!Box DNS oder Pi-hole)

# Portainer Web-UI für Kubernetes Cluster Management
PORTAINER_HOST=portainer.privat.de

# Longhorn UI für Kubernetes Storage Management
LONGHORN_HOST=longhorn.privat.de

# Grafana für Monitoring Dashboards
GRAFANA_HOST=grafana.privat.de

# Prometheus für Metrics & Alerts
PROMETHEUS_HOST=prometheus.privat.de

# === Grafana Admin Passwort ===
# Wird beim Helm-Deploy als Initialpasswort gesetzt (User bleibt admin)
GRAFANA_ADMIN_PASS=changeme

# === Optional: DynDNS/Extern ===
# Wird aktuell im Setup nicht benötigt, aber für spätere Roadmap-Features wie automatisches Let's Encrypt nutzbar
# DYNHOST_FQDN=my-dyndns.privat.de

# === Optional: k3s Cluster Labels ===
# Cluster-spezifische Metadaten, z. B. für Monitoring-Anwendungen
# CLUSTER_NAME=homelab-k3s
# ENVIRONMENT=homelab
# TEAM=devops

# === Hinweis für IONOS User ===
# Stelle sicher, dass deine Subdomains (*.privat.de) bei IONOS auf deine öffentliche WAN-IP zeigen.
# Für lokale Auflösung im Homelab empfehlen sich Fritz!Box DNS-Einträge oder Pi-hole für die MetalLB IP (z. B. 192.168.200.240).

# === Auto-Restart Hook aktiv ===
# Dieses Setup erzwingt Auto-Restarts für Ingress-NGINX, Portainer, Longhorn & Monitoring via Helm podAnnotations."auto-restart"
# Bei jedem Helm-Upgrade wird der Rollout der jeweiligen Deployments automatisch getriggert.

